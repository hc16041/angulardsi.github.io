import { BusServicioPipe } from './bus-servicio.pipe';

describe('BusServicioPipe', () => {
  it('create an instance', () => {
    const pipe = new BusServicioPipe();
    expect(pipe).toBeTruthy();
  });
});
