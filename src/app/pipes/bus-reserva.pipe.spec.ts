import { BusReservaPipe } from './bus-reserva.pipe';

describe('BusReservaPipe', () => {
  it('create an instance', () => {
    const pipe = new BusReservaPipe();
    expect(pipe).toBeTruthy();
  });
});
